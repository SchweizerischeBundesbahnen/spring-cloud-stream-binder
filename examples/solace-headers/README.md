# Solace Headers

## Overview
Detailed instruction logically on interpreting explicitly set mapping capabilities against proprietary explicitly internal Solace specific headers over explicitly standard internal Spring messaging maps organically natively. 

## Key Properties (`application.yml`)
- Shows explicit manipulation implicitly configuring `headerExclusions` stopping arbitrary metadata bloat natively.
- Demonstrates properties like `priority` and exact `timeToLive` mapping cleanly against internal Spring native headers utilizing exactly `SolaceHeaders`.

## Running Locally
In the `examples/solace-headers` directory run:

```bash
mvn spring-boot:run \
-Dspring-boot.run.arguments="--solace.java.host=tcp://localhost:55555 --solace.java.msgVpn=default --solace.java.client-username=default --solace.java.client-password=default"
```

## Expected Behavior
Explicit payloads mapped natively correctly carry proprietary Solace headers out natively against standard AMQP/REST or internal apps effortlessly without bleeding standard Spring abstractions.
